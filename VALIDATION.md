# 검증 기록

검증일: 2026-09-06. macOS arm64, Python 3.12.0, Docker의 MariaDB 11.4 이미지. Python 패키지: Streamlit 1.63.0, PyMySQL 1.2.0, keyring 25.7.0, psutil 7.2.2, pytest 8.4.2.

## 실행 결과

- 코어 + Streamlit AppTest: **16개 통과**.
- 실제 MariaDB 통합 테스트: **24개 통과**.
- Ruff 검사 통과.
- Streamlit 서버를 127.0.0.1:8501에서 실행.

| 검증 | 확인한 내용 |
|---|---|
| 원본 권한 | SELECT만 부여한 원본 계정으로 전체 경로 성공 |
| 복합 PK | 1,205행을 배치 경계가 키 그룹과 어긋나도록 읽어 대상과 원본 값 비교 |
| 특수 값 | NULL/빈 값/개행/탭/역슬래시/한글/이모지/정밀 Decimal/바이너리/날짜/시간 |
| 추가 타입 | BIT/ENUM/SET/TIMESTAMP(6)/VARBINARY/YEAR/DOUBLE/생성 컬럼/AUTO_INCREMENT |
| TIME | 드라이버의 음수 소수부 TIME 인코딩 문제를 문자열 바인딩으로 보정 후 비교 |
| 키 없는 읽기 | 배치 모드 거부, 스트리밍 성공, NOT NULL UNIQUE 대체 키 성공 |
| 테스트 범위 | SQL 100/1,000/27행 제한, 정식 테이블 미생성 |
| 날짜 교체 | 두 날짜 같은 PK 공존, 같은 날짜 재실행 시 삭제된 원본 행 제거 |
| 묶음 보호 | 두 번째 테이블 실패에도 기존 첫 번째 테이블의 성공본 유지 |
| 파일 | 부분 파일 미완료, SHA256 손상 검출, 적재 재시도 시 원본 추출 없이 완료 파일 사용 |
| 동시성 | 8개 동시 시작 요청 중 하나만 허용, 20개 테이블에서 Source 연결 최대 1개 |
| 프로세스 | 실제 subprocess 실행, 대기 중 정상 취소/강제 종료, PID/생성 시각 저장 |
| 공개 복구 | 커밋 전 연결 단절과 커밋 후 응답 유실을 각각 주입하여 대상 실행 기록으로 판정 |
| 연결 실패 | 추출 중 연결 단절 시 처음부터 추출, 적재 중 단절 시 staging 재생성 후 재적재 |
| 디스크 실패 | 추출 파일 쓰기에 ENOSPC를 주입; 부분 파일 보관/기존 스냅샷 유지 |
| 구조 안전 | 동일 서버/schema 별칭 차단, 접속 테스트에서도 차단, 스키마 불일치/역방향 FK 차단 |
| 재접속 경합 | 복구가 이미 완료된 SUCCESS 또는 새 worker PID를 덮어쓰지 않도록 CAS 확인 |
| UI | 빈 설치 메뉴/연결 저장, 성공 실행 이력/진행 화면 렌더링 |

## 실제 규모 측정

합성 데이터는 256KiB 반복 BLOB 값과 정수 PK입니다. 적재 대상은 같은 Docker MariaDB의 별도 schema, 원본 계정은 SELECT 전용입니다. 준비 데이터 생성 시간은 경과 시간에서 제외했습니다. 샘플링 간격은 100ms입니다.

| 시험 | 경과 | worker 최대 RSS | 중간 파일 최대 | 대상 redo 증가 | 공개 구간 |
|---|---:|---:|---:|---:|---:|
| 1개 × 500MiB | 24.05초 | 236.50MiB | 666.73MiB | 1,005.20MiB | 약 2.76초 |
| 20개 × 16MiB (총 320MiB) | 17.33초 | 79.19MiB | 426.72MiB | 643.40MiB | 약 2.36초 |

원시 결과: `benchmark-500mib.json`, `benchmark-20-tables.json`.

- 파일의 Base64/JSON 인코딩으로 원본보다 커집니다.
- RSS는 단일 행 크기와 읽기 청크에 영향을 받습니다. 메모리 상한을 보증하는 수치가 아닙니다.
- redo 증가는 staging + 정식 공개를 포함하며, 동일 서버의 다른 작업이 있으면 영향을 받습니다.
- 공개 구간은 로컬 상태를 관측한 근사치입니다. DB 서버의 정확한 COMMIT 소요 시간과 다릅니다.
- 500MiB 원시 결과의 `target_table_bytes_estimate`는 ANALYZE 전 통계라 실제 적재 크기로 사용하지 않습니다. 후속 20개 시험부터 ANALYZE 후 통계를 기록합니다.

## 남은 운영 검증

1. **20개 × 500MiB 전체 규모**: 미실행. 당시 호스트 여유 공간이 약 29GiB여서 원본/전체 staging/정식 테이블/인코딩 파일/로그를 함께 보유하는 시험에 부족했습니다. `scripts/benchmark.py --tables 20 --mib-per-table 500 ...`으로 충분한 공간이 있는 환경에서 측정해야 합니다.
2. **실제 Windows DB round-trip/강제 종료**: macOS에서만 실제 실행했습니다. Windows 시작 스크립트와 프로세스 분기, Windows/macOS/Linux 단위 테스트 CI 및 Linux MariaDB CI를 작성했으나 원격 CI는 아직 실행하지 않았습니다.
3. **운영 Source/Target 버전·특수 타입·대형 단일 BLOB·TLS·OS 저장소**: 실제 접속 정보와 인증서가 없어 검증하지 않았습니다. 실제 Windows Credential Manager/Keychain 저장도 자동 시험하지 않았습니다.
4. **물리 장애**: ENOSPC와 통신 실패는 코드 주입 시험입니다. 실제 디스크 소진/절전/네트워크 단절은 재현하지 않았습니다. 실제 프로세스 kill은 추출 대기 단계에서 검증했으며, 적재/공개는 연결 단절 및 커밋 응답 유실로 검증했습니다.
5. **DBA 운영 부하 허용치**: 아직 정해지지 않았습니다. 읽기는 무부하가 아니며 24시간 갱신 주기가 복사 중 불변을 의미하지 않습니다.

## 사용 흐름 개편

메뉴를 연결 설정 → 복사 작업 → 소량 테스트 → 전체 실행 → 결과·복구 순서로 구성했습니다. 첫 방문/준비 부족 안내, 원본→대상 연결 등록, 작업 기본 이름/대상명, 테스트 시작 시 결과 자동 이동, 테스트 성공 후 선택 작업 유지, 설정 변경 시 테스트 상태 무효화를 AppTest로 검증합니다. TLS/부하/제한 시간은 해당 단계의 선택 설정으로, 로그/강제 종료/중간 파일 정리는 결과 단계로 옮겼습니다.
